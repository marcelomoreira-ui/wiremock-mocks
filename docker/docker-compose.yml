services:
  wiremock:
    image: wiremock/wiremock:3.13.2-1
    container_name: wiremock
    ports:
      - "8089:8080"
    volumes:
      - ../mappings:/home/wiremock/mappings
      - ../files:/home/wiremock/__files
    command:
      - "--global-response-templating"